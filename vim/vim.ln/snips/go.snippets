snippet ttmap "A table test using a map" b
func Test${1:Function}(t *testing.T) {
	tests := map[string]struct{
		Input    string
		Expected string
	}{
		"${2:test1}": {},
	}

	for name, test := range tests {
		t.Run(name, func(t *testing.T) {
			${0:${VISUAL}}
		})
	}
}
endsnippet
